<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Responsive Leaderboard</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link rel="icon" href="/logo.png" type="image/png">
    <link rel="stylesheet" href="..//leaderboard.css">
</head>
<body>
   
    <button type="button" class="btn btn-floating btn-lg" id="btn-back-to-top">
        <i class="fas fa-arrow-up"></i>
    </button>

   
    <nav class="p-6 shadow-lg" style="background-color: #4a90e2;">
        <div class="max-w-7xl mx-auto flex justify-between items-center flex-wrap">
            
            <h1 class="text-white text-3xl md:text-4xl font-bold tracking-wide">Leaderboard</h1>
            
            <% const url = 'main?username=' + encodeURIComponent(username); %>
            <button 
                class="mt-4 md:mt-0 bg-white text-indigo-700 px-4 md:px-6 py-2 text-sm md:text-base rounded-lg font-semibold hover:bg-indigo-100 transition-all"
                id="playAgainBtn"
                onclick="window.location.href='<%= url %>'">
                Play again
            </button>
        </div>
    </nav>    

    <div class="users">
       
        <div class="search-container">
            <i class="fas fa-search search-icon"></i>
            <input type="text" id="searchInput" placeholder="Search for usernames..." onkeyup="filterUsers()">
        </div>

        <div id="noUserMessage" style="display: none; margin-bottom: 20px; color: #e74c3c;">No existing player with this name.</div>

        <ul id="userList">
            <% const colors = ['#e74c3c', '#3498db', '#2ecc71', '#f29143']; %>
            <% let topUser = users.reduce((prev, current) => (current.maxLevel > prev.maxLevel) ? current : prev); %>
            <% users.forEach((user, index) => { 
                const color = colors[index % colors.length];
                const isTopUser = user.username === topUser.username;
            %>
            <li onclick="location.href='/history?user=<%= encodeURIComponent(user.username) %>'" style="background-color: <%= isTopUser ? 'gold' : color %>;">
                <i class="fas fa-user"></i> 
                <span><%= user.username %></span> 
                <span style="margin-left: auto; color: #000000;"><%= user.maxLevel %></span>
                <% if (isTopUser) { %>
                <i class="fas fa-medal"></i>
                <% } %>
            </li>
            <% }) %>
        </ul>
    </div>

    <script>
        const backToTopButton = document.getElementById('btn-back-to-top');
        const userList = document.getElementById('userList');
        const noUserMessage = document.getElementById('noUserMessage');

        // Show or hide the back-to-top button
        window.onscroll = function() {
            if (document.body.scrollTop > 20 || document.documentElement.scrollTop > 20) {
                backToTopButton.classList.add('show');
            } else {
                backToTopButton.classList.remove('show');
            }
        };

        // Scroll to the top of the page
        backToTopButton.addEventListener('click', function() {
            window.scrollTo({ top: 0, behavior: 'smooth' });
        });

        // Function to filter users based on input
        function filterUsers() {
            const input = document.getElementById('searchInput');
            const filter = input.value.toLowerCase();
            const li = userList.getElementsByTagName('li');
            let foundUser = false; // Track if any user was found

            for (let i = 0; i < li.length; i++) {
                const txtValue = li[i].textContent || li[i].innerText;
                if (txtValue.toLowerCase().indexOf(filter) > -1) {
                    li[i].style.display = "";
                    foundUser = true; // Set foundUser to true if a match is found
                } else {
                    li[i].style.display = "none";
                }
            }

            // Show or hide the "no user" message
            noUserMessage.style.display = foundUser ? "none" : "block";
        }
    </script>

    <style>
        
        .top-user-gold {
            background: linear-gradient(145deg, #f0e68c, #f7d15a 20%, #ffcc33 40%, #f5b500 60%, #db9500 80%, #b67c00 100%);
        }

        .top-user-silver {
            background: linear-gradient(145deg, #d6d6d6, #cccccc 20%, #bfbfbf 40%, #b2b2b2 60%, #999999 80%, #808080 100%);
        }

        .top-user-bronze {
            background: linear-gradient(145deg, #c19a6b, #b07e52 20%, #a96a4f 40%, #925d42 60%, #7b4930 80%, #603912 100%);
        }

        
        .random-user-bg:hover {
            transform: scale(1.05);
        }
    </style>

</body>
</html>
